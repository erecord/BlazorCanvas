public static class GameObjectExtensions
{
    // public static GameObject SetAsset(this GameObject gameObject, ref IAssetsResolver assetsResolver, string assetPath)
    // {
    //     var spriteRenderComponent = gameObject.Components.Get<SpriteRenderComponent>();
    //     var sprite = assetsResolver.Get<Sprite>(assetPath);
    //     spriteRenderComponent.Sprite = sprite;

    //     var bbox = gameObject.Components.Get<BoundingBoxComponent>();
    //     bbox.SetSize(sprite.Bounds.Size);

    //     return gameObject;
    // }

    // public static GameObject SetPosition(this GameObject gameObject, float x, float y)
    // {
    //     var transformComponent = gameObject.Components.Get<TransformComponent>();
    //     transformComponent.SetPosition(x, y);
    //     return gameObject;
    // }
}